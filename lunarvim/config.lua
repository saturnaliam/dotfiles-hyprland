-- MAJOR CONFIG FILES --
reload("plugins") -- installation + configuration of plugins
reload("options") -- general options (colorscheme, tab width, etc.)
reload("keybinds") -- keybinds

-- MINOR CONFIG FILES --
reload("extra.lualine") -- lualine configuration
reload("extra.alpha") -- alpha configuration
